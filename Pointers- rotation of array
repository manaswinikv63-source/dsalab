#include <iostream>
#include <vector>
using namespace std;

void rotate_array(vector<int>& arr, int n, int k) {
    k = k % n;
    vector<int> rotated(n);

    for (int i = 0; i < n; i++) {
        rotated[(i + k) % n] = arr[i];
    }

    for (int i = 0; i < n; i++) {
        cout << rotated[i] << " ";
    }
}

int main() {
    int n, k;
    cin >> n;
    vector<int> arr(n);
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }
    cin >> k;

    rotate_array(arr, n, k);
    return 0;
}
